# Docker Support for Pano

## Tasks

- [x] Create `.dockerignore` to exclude node_modules, dist, and dev artifacts
- [ ] Create multi-stage `Dockerfile` (build frontend + production image with Express server)
- [ ] Update `.gitignore` to include Docker-related entries
- [ ] Verify Docker build succeeds
